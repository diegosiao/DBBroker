{
  "$id": "https://example.com/openmsi.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Schema for OpenMSI package definition",
  "type": "object",
  "properties": {
    "package": {
      "description": "The schema version used for this JSON document.",
      "type": "object",
      "required": true,
      "properties": {
        "name": { "type": "string", "description": "Required. Package name", "required": true },
        "version": { "type": "string", "description": "Required. Package version.", "required": true },
        "manufacturer": { "type": "string", "description": "Required. Package manufacturer or Author.", "required": true },
        "productCode": { "type": "string", "description": "Required. A GUID identifying the package product. ProductCode changes with major upgrades but may be the same for different application versions.", "required": true },
        "upgradeCode": { "type": "string", "description": "Required. A GUID identifying a product version. UpgradeCode stays constant across all versions of the same application. ", "required": true }
      }
    },
    "ui": {
      "description": ".",
      "type": "object",
      "properties": {
        "template": { 
          "type": "string", 
          "description": "Required. Pre-defined UI installation wizards. Check the documentation for details about all available options. Default 'basic'.", 
          "enum": ["basic", "settings", "license", "licenseAndSettings", "custom"],
          "required": true
        },
        "templateSettings": {
          "description": "Optional. If the template specified require settings this property is used to specify them.",
          "type": "array",
          "items": {
            "type": "object"
          }
        }
      }
    },
    "shortcuts": {
      "description": "Specify which application shortcuts should be create for the application",
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "target", "location"],
        "properties": {
          "name": { "type": "string", "description": "Required. Shortcut name displayed to the user.", "required": true },
          "description": { "type": "string", "description": "Details displayed to the user." },
          "target": { "type": "string", "description": "The installed file this shortcut should point to.", "required": true },
          "location": { "type": "string", "description": "Required. ", "enum": ["Desktop", "MenuFolder"], "required": true }
        }
      }
    }
  }
}
