{
  "$id": "https://example.com/openmsi.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Schema for OpenMSI package definition",
  "type": "object",
  "properties": {
    "package": {
      "description": "The schema version used for this JSON document.",
      "type": "object",
      "required": ["name", "version"],
      "properties": {
        "name": { "type": "string", "description": "Required. Package name" },
        "version": { "type": "string", "description": "Required. Package version." },
        "manufacturer": { "type": "string", "description": "Required. Package manufacturer or Author." },
        "productCode": { "type": "string", "description": "Required. A GUID identifying the package product. ProductCode changes with major upgrades but may be the same for different application versions." },
        "upgradeCode": { "type": "string", "description": "Required. A GUID identifying a product version. UpgradeCode stays constant across all versions of the same application. " }
      }
    },
    "files": {
      "description": "The application files to be deployed.",
      "type": "array",
      "items": {
        "type": "object",
        "required": ["source"],
        "properties": {
          "source": { "type": "string", "description": "Directory or file to included. The path is relative to this file. Do not use absolute paths." },
          "target": { "type": "string", "description": "Can only be used when a file is specified in 'source'. Changes the default target directory and/or file name." },
          "exclude": { 
            "type": "array", 
            "description": "Used only be specified when a directory is specified in 'source'. A list of files or directories relative to this file to be excluded from source. Do not use absolute paths.", 
            "items": {
              "type": "string"
            }
          }
        }
      }
    },
    "ui": {
      "description": ".",
      "type": "object",
      "properties": {
        "template": { 
          "type": "string", 
          "description": "Required. Pre-defined UI installation wizards. Check the documentation for details about all available options. Default 'basic'.", 
          "enum": ["basic", "settings", "license", "licenseAndSettings", "custom"]
        },
        "templateSettings": {
          "description": "Optional. If the template specified require settings this property is used to specify them.",
          "type": "array",
          "items": {
            "type": "object"
          }
        }
      }
    },
    "shortcuts": {
      "description": "Specify which application shortcuts should be create for the application",
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "target", "location"],
        "properties": {
          "name": { "type": "string", "description": "Required. Shortcut name displayed to the user." },
          "description": { "type": "string", "description": "Details displayed to the user." },
          "target": { "type": "string", "description": "The installed file this shortcut should point to." },
          "location": { "type": "string", "description": "Required. ", "enum": ["Desktop", "MenuFolder"] }
        }
      }
    }
  }
}
